# 3Dify Tray Monitor Config
# Only SERVER_URL is read from environment. Credentials are entered
# via the login UI and stored encrypted via OS keychain (safeStorage).
SERVER_URL=https://your-server.example.com

# Local worker management (Start/Stop Worker from tray)
# Set these to enable launching the WSL worker process from the tray app.
TRAY_WSL_DISTRO=Ubuntu-22.04
TRAY_CONDA_SH=/path/to/miniconda3/etc/profile.d/conda.sh
TRAY_CONDA_ENV=hunyuan3d
TRAY_WORKER_DIR=/path/to/worker
