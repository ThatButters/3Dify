# ─── VPS Connection ──────────────────────────────────────────
# WebSocket URL of your 3Dify server
VPS_WS_URL=wss://yourdomain.com/ws/worker

# Must match WORKER_AUTH_TOKEN on the server
WORKER_AUTH_TOKEN=same-token-as-server

# ─── Local Paths ─────────────────────────────────────────────
# Path to the img2stl.py script and Hunyuan3D repo
# (defaults work if worker/ is inside the 3Dify repo)
# IMG2STL_DIR=/home/you/projects/3Dify
# HUNYUAN3D_REPO=/home/you/projects/3Dify/Hunyuan3D-2.1

# ─── GPU Thresholds ──────────────────────────────────────────
# Minimum free VRAM to accept a job (GB)
# MIN_FREE_VRAM_GB=10.0
# MIN_FREE_VRAM_GB_LOADED=2.0
# MAX_GPU_UTIL_PCT=15

# ─── Logging ─────────────────────────────────────────────────
# LOG_DIR=./logs
